<script>
    var nameFile = JSON.parse('<%- JSON.stringify(nameFile)%>');
</script>
<div class="wrapper">
    <div>
        <section class="content">
            <div class="container-fluid">

                <div class="card">
                    <div class="card-header ">
                        <div class="d-flex justify-content-end">
                            <button type="button" class="btn btn-outline-danger btn-sm mr-2" id="btn-upload-logo">
                                Hủy
                            </button>
                            <button type="button" id="btn-upload-logo" class="btn btn-primary btn-sm">
                                Lưu
                            </button>
                        </div>
                    </div>
                    <div class="card-body">

                        <div class="custom-file col-sm-6">
                            <input type="file" class="custom-file-input" id="customFile" value="<%- logoPath %>"
                                class="validate[required]">
                            <label class="custom-file-label text-center" for="customFile">
                                <div class="default-text text-center <%- nameFile ? 'd-none' : '' %> ">
                                    <span class="font-weight-light"> <i class="fas fa-cloud-upload-alt"></i> Kéo
                                        ảnh vào box hoặc</span>
                                    <span class="text-primary">chọn từ máy</span>
                                </div>
                                <% if(nameFile){ %>
                                    <span class="name-file-logo">
                                        <%- nameFile %>
                                    </span>
                                    <% } %>
                            </label>
                        </div>
                        <div style="width: 500px;">
                            <img id="previewImg" src="<%- imageLink %>" alt="logo" class="brand-image mt-4"
                                style="width: 80%; margin: 2px 0.3rem; max-height: 110px;">
                            <i class="fa fa-trash" aria-hidden="true" title="Click để xóa"></i>
                        </div>

                        <!-- <div class="">
                            <form id="fileUploadForm" action="tenant/uploadLogo" enctype="multipart/form-data"
                                method="POST">
                                <span>Upload Profile Picture:</span>
                                <input type="file" name="mypic" id="hidden-value" required /> <br>
                                <input type="submit" value="submit" id="submitform">
                            </form>
                        </div> -->
                    </div>
                </div>
            </div>
            <!-- /.container-fluid -->
        </section>
        <!-- /.content -->
    </div>
</div>